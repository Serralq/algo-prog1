#include <bits/stdc++.h>

void solve() {
    int n;
    std::cin >> n;
    while (n--) {
        std::string s;
        std::cin >> s;
        std::cout << s << std::endl;
    }
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    
    solve();
}
